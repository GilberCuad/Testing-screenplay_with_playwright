version: '3.9'

services:
  playwright:
    build: .
    container_name: playwright-diggi-pymes
    volumes:
      - ./:/app
      - /app/node_modules
    working_dir: /app
    environment:
      - CI=true
      - PLAYWRIGHT_BROWSERS_PATH=/ms-playwright
      - HEADLESS=true
    tty: true